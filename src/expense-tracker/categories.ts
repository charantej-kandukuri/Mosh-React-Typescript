const categories = ["Groceries", "Utilities", "Entertainment"] as const;

export default categories;

/**
 * ! We have moved the categories to a new file because,
 * we can use it in any component without worring about ordering of imports
 */
